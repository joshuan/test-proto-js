syntax = "proto3";

package helloworld;

message Person {
    string name = 1;
    int32 age = 2;
}

message Greeting {
    string message = 1;
    repeated Person people = 2;
}

service HelloWorldService {
    rpc SayHello (Person) returns (Greeting);
}